server:
  port: 2706

logging:
  level:
    com.seedotech.dddhexagonalarchitecture: info

spring:
  jpa:
    show-sql: false
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=- 1;DB_CLOSE_ON_EXIT=FALSE
    username: test
    password: test