type Order {
    id: ID,
    name: String,
    description: String,
    total: Int
}

input OrderGraphQLInput {
    name: String!,
    description: String!,
    total: Int!
}

type Query {
    order(id: ID!): Order
}

type Mutation {
    createOrder(orderGraphQLInput: OrderGraphQLInput): Order!
}